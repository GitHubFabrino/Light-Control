import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  cardTime: {
    borderWidth: 1,
    borderBlockColor: "black",
    width: 50,
    alignItems: "center",
    borderRadius: 4,
  },
});
